[project]
name = "changelake"
version = "0.1.0"
description = "ChangeLake Phase 6 — Backfill / G7 on MySQL + Flink CDC + Paimon"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
