[project]
name = "changelake"
version = "0.10.0"
description = "ChangeLake Phase 10 — MySQL CDC → Flink → Paimon lakehouse (G1–G10 + engineering)"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
