G10 Compaction / Phase 9
table=ods.ods_compact_demo
status=NOT RUN (fill by scripts/compaction.sh on a local Docker stack)
method=CALL sys.compact('ods.ods_compact_demo', '', '', '', 'sink.parallelism=1', '', '', 'full')
flink=1.18.1 positional; paimon=1.4.2; compact_strategy=full; write-only inserts
hard_gate=fingerprint_before == fingerprint_after
soft_expect=file_count reduced (do not hard-fail if noisy)
NOT claimed: production sizing SLA / EO-2PC / Exactly-Once E2E / latency % drop / continuous CDC compaction
